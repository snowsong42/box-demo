# 🧠 内存管理纪律

> 涵盖 box-demo 全部组件的内存分配模型、防泄漏策略和压力测试方法。

---

## 1. 分配层级（优先级从高到低）

| 优先级 | 方式 | 适用场景 | 例 |
|--------|------|----------|-----|
| **1** | 栈变量 | < 2KB，生命周期明确 | `int32_t raw[256]` |
| **2** | 静态 buffer | 组件级全局状态 | `s_text[16384]` |
| **3** | PSRAM (`heap_caps_malloc`) | > 10KB，非 DMA | `s_marquee_raw` 150KB |
| **4** | 堆 (`malloc`) | 临时动态大小 | `chat_task` 的 HTTP 响应 |

> **铁律**：能用栈不用堆，能用静态不用动态，能用 PSRAM 不用 DRAM。

---

## 2. FreeRTOS 任务参数铁律

```c
// ✅ 正确：任务体负责释放 strdup 参数
void my_task(void *arg) {
    char *data = (char *)arg;
    // ... 使用 data ...
    free(data);  // ← 必须！
    vTaskDelete(NULL);
}

// ❌ 错误：永不释放
void my_task(void *arg) {
    char *data = (char *)arg;
    // ... 使用 data ...
    vTaskDelete(NULL);  // data 泄漏！
}
```

### 本项目已知泄漏（已全部修复）

| 位置 | 泄漏物 | 触发 |
|------|--------|------|
| `asr.cpp:upload_task` | `strdup(path)` | 每次上传 |
| `network.cpp:ping_test_task` | `strdup(host)` | 每次 Ping |
| `network.cpp:http_test_task` | `strdup(url)` | 每次 HTTP |
| `network.cpp:network_test_stop` | `vTaskDelete` 强杀 → socket/句柄泄漏 | 每次 BACK |

---

## 3. 逐组件内存模型

### 3.1 Chat 组件

```
s_text[16384]    静态 DRAM  对话历史（~100 轮）
s_reply[2048]    静态 DRAM  单次 AI 回复
s_audio          malloc      单次 TTS 音频（~50KB 峰值）
HTTP data        malloc      单次响应（~30KB 峰值）
```

**生命周期**：

```
chat_send() → strdup(text)  ──→ chat_task → free(send_text)  ✅
                           ──→ malloc(data) → free(data)       ✅
                           ──→ malloc(s_audio) → chat_free_audio()  ✅
                                                    ↓
                          main.cpp Phase 4: 保存文件 → chat_free_audio()
                          chat_text_clear(): free(s_audio)    ✅
```

**用后即清原则**：`s_audio` 在保存到 SD 卡后立即 `chat_free_audio()`，不保留到下一轮。

### 3.2 ASR 组件

```
s_text[2048]        静态 DRAM  识别结果
s_last_result[512]  静态 DRAM  单次结果
data                malloc      上传时整个 WAV 文件（~600KB）
```

**生命周期**：

```
asr_upload() → strdup(path)  ──→ upload_task → free(path)    ✅
                            ──→ malloc(fsize) → free(data)    ✅
                            ──→ esp_http_client_init → cleanup ✅
```

### 3.3 Record 组件

```
raw[256]         栈 (1024B)   I2S 读取缓冲
pcm[256]         栈 (512B)    PCM 转换输出
placeholder[44]  栈 (44B)     WAV 头占位
```

**全部栈分配，零堆分配**。录音循环中最重的操作是 `fwrite` 到 SD 卡。

### 3.4 Audio 组件

```
buf[512]         栈 (1024B)   WAV 播放缓冲
```

循环 `fopen → fread → I2S write → fclose → fopen`。单文件播放，无堆分配。

### 3.5 Display 组件

| 资源 | 分配 | 大小 | 生命周期 |
|------|------|------|----------|
| `s_backbuffer` | PSRAM Sprite | 320×240×2=150KB | 全局 |
| PNG 解码 buf | `heap_caps_malloc`(PSRAM) | ~200KB | 临时，每次 `heap_caps_free` |
| GIF 帧 Sprite ×28 | PSRAM | ~2.24MB | 进入 GIF 模式一次 |
| `s_marquee_raw` | `heap_caps_malloc`(PSRAM) | 150KB | 进入走马灯一次 |

> GIF 帧和走马灯数据在退出对应模式时不释放（有意缓存）。如需释放，在状态切换时调用 `free_gif_frames()` / `heap_caps_free(s_marquee_raw)`。

### 3.6 Storage / SD Card 组件

```
s_card           全局指针   sdmmc_card_t（由 FATFS 管理）
SPIFFS mount      系统      内部缓存由 CONFIG_SPIFFS_CACHE 控制
```

SD 卡 `sd_card_init()` 只执行一次。`sd_card_speed_test()` 中 `malloc(64KB)` 用于测速数据块，用后即 `free`。

### 3.7 Network 组件

```
host_copy/url_copy   strdup   ~30B   ping/http task 参数
params               strdup   ~30B   tcp task 参数
JSON buf             calloc   ~1KB   WiFi 扫描结果
```

全部已在任务体中 `free()`。`network_test_stop()` 改为优雅退出（先设标志，等待 500ms，最后手段才 `vTaskDelete`）。

---

## 4. 缓冲大小设计指南

| 缓冲 | 大小 | 推算方法 |
|------|------|----------|
| `s_text` (chat) | 16384 | 每轮 ~150 字节 × 100 轮 ≈ 15KB |
| `s_text` (asr) | 2048 | 每句 ~80 字节 × 20 句 ≈ 1.6KB |
| `s_reply` | 2048 | DeepSeek max_tokens=300 → ~1800 字节 |
| `s_audio` | ~50KB | TTS 语音 2-3 秒 × 44100×2 = ~264KB 未压缩 |
| I2S DMA | ~4KB | 默认 4 desc × 1024 字节 |

---

## 5. 压力测试方法

### 5.1 Chat 对话压力测试

```
循环 100 次:
  1. 录音 3 秒 → ASR 识别
  2. chat_text_append_user()
  3. chat_send() → 等待回复
  4. chat_text_append_ai()
  5. chat_free_audio()

每 10 次记录:
  esp_get_free_heap_size()
  esp_get_free_psram_size()
```

**预期**：堆内存波动 < 5KB（静态 buffer 外无累积泄漏）。

### 5.2 快速定位泄漏

```c
int before = esp_get_free_heap_size();
do_something();
int after = esp_get_free_heap_size();
if (before != after) ESP_LOGW(TAG, "LEAK? %d bytes", before - after);
```

---

## 6. SPIFFS vs SD 卡 — 内存影响

| | SPIFFS | SD 卡 FAT |
|------|------|------|
| 写入路径 | SPIFFS 内部缓存 + GC | FAT 表更新 (几乎零内存) |
| GC 峰值内存 | 擦除块时额外 ~4KB | 无 GC |
| 文件删除 | 标记脏页，不立即回收 | 立即释放簇 |
| 适合 | 小文件 (< 100KB) | 流式写入、大文件 |

> 本项目录音和资源已全部迁到 SD 卡。SPIFFS 仅保留 NVS 配置。

---

## 7. 常见反模式

| 反模式 | 后果 | 正确做法 |
|--------|------|----------|
| 循环内 `malloc` 不配对 `free` | 内存快速耗尽 | 配对或移出循环 |
| `vTaskDelete` 强杀任务 | 任务清理代码不执行 | 设标志位 → 等待 → 最后手段强杀 |
| 静态指针不初始化 | 野指针 | `static X *ptr = nullptr;` |
| 大 buffer 用栈 | 栈溢出 + 任务崩溃 | > 2KB 用堆或静态 |
| `strdup` 不检查返回值 | NULL 指针崩溃 | 始终 `if(!ptr) return;` |

---

## 8. 相关文件

- `components/chat/chat.cpp` — Chat 内存模型实现
- `components/asr/asr.cpp` — ASR 上传内存
- `components/record/record.cpp` — 纯栈录音
- `components/display/display.cpp` — PSRAM 精灵管理
- `docs/guides/内存与泄漏排查.md` — 已修复泄漏清单
